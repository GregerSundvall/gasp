
#include "UISystem.h"

namespace UI {
    UISystem::UISystem() {
        // int screenWidth = GetScreenWidth();
        // int screenHeight = GetScreenHeight();
        // uiComponents.emplace_back();
        // uiComponents.back().position = {5, 5};
        // uiComponents.back().size = {static_cast<float>(screenWidth -5), static_cast<float>(screenHeight -5)};
    }

    void UISystem::AddChildComponentTo(const int parentID) {

    }


    void UISystem::Draw() {
        // for (const std::vector<UIComponent>& areas : uiAreas) {
        //     for (const UIComponent& area : areas) {
        //         DrawRectangleLines(area.position.x, area.position.y, area.size.x, area.size.y, area.color);
        //     }
        // }
    }

}